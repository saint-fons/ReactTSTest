import React from "react";
import ReactDOM from "react-dom";
import ContextHook from "./Hooks/ContextProvider";

ReactDOM.render(
  <React.StrictMode>
    <ContextHook />
  </React.StrictMode>,
  document.getElementById("root")
);
